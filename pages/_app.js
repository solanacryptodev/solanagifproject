import '../styles/globals.css';
//import Layout from '../components/layout/layout';

function MyStyles({ Component, pageProps }) {
    return (
        <Component {...pageProps} />
    );
}

export default MyStyles;